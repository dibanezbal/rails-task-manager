<h1>My tasks</h1>
 <% @tasks.each do |task| %>
    <%= check_box_tag 'tasks', 'ok', task.completed %>
    <%= link_to task.title, task_path(task) %>
<br>
<% end %>

<%= link_to "Add a new task", tasks_new_path %>

